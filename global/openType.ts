export type openType = {
  open: boolean;
  setOpen: (state: boolean) => any;
};

export type editOpenType = {
  editOpen: boolean;
  setEditOpen: (state: boolean) => any;
};

export type selectedItem = {
  selectedItem: any;
  setSelectedItem: (item: any) => any;
};
